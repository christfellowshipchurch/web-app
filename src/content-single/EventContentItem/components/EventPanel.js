import React from "react";
import PropTypes from "prop-types";

const EventPanel = ({}) => (
  <div className="col-12 col-lg-4 bg-warning">[ EventPanel component ]</div>
);

EventPanel.propTypes = {};

EventPanel.defaultProps = {};

export default EventPanel;
